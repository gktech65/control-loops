(1)
# Write a Python program for checking the given number is even or odd 
num = int(input())
if (num % 2) == 0:
   print("Even")
else:
   print("Odd")

(2)
#Write a program for finding biggest number among 3 numbers
a=2
b=4
c=5
if a>b&a>c:
  print("2")
elif b>a&b>c:
    print("4")
else:
      print("5")

(3)
#Implement python program to read person's age from keyboard and display whether he is eligible for voing or not
age=int(input())
if age>=18:
  print("Eligible for Voting")
else:
    print("Not Eligible for Voting")

(4)
# Write a python program to check the given year is leap year or not
year=int(input())
if(year%4==0 and year%100!=0 or year%400==0):
    print("Leap Year")
else:
    print("Not a Leap Year")

(5)
# Write a program to create first N natural numbers without using range()
num=int(input())
i=1;
while i<=num:
    print(i),
    i=i+1

(6)
# Write a python program to print sum of N natural numbers
n=int(input())
sum=0
i=1
while i<=n:
 sum=sum+i
 i=i+1
print(sum)

(7)
# Write a Python program to find factorial of a number
num=int(input())
fact=1
i=1
while i<=num:
    fact=fact*i
    i=i+1
print(fact)

(8)
# Find numbers which re divisible by 7 and multiple of 5 between a given range
start = int(input())
end = int(input())
cnt = start

while cnt <= end:

    if(cnt % 35 == 0):
        print(cnt)

    cnt += 1

(9)
# Write a python program to construct the following pattern, using a nested for Lookup 
# *
# *  *
# *  *  *
# *  *  *  *
# *  *  *  *  *
# *  *  *  *
# *  *  *
# *  *
# *
H = 5

for i in range(1,2*H):
  if i > H:
    i = 2*H - i
  for j in range(i):
    print("*",end = "  ")
   
  print("")

(10)
#Write a program that accepts the lengths of three sides of a triangle as inputs. The program output should indicate whether the triangle is 
#A. Equilateral triangle 
#B. Right angle triangle 
#C. Doesn’t come in both categories
a=int(input())
b=int(input())
c=int(input())
if(a==b and a==c):
 print("Equilateral Triangle")
elif(c**2==((b**2)+(a**2))):
  print("Right Angle Triangle")
else:
  print("Doesn't come in both cases")

(11)
# Write a Python program to perform the following logical operations. Give Precedence order of logical operators. 
#Given x=True, y=False
# A. x and not y
# B. not x and y 
# C. not x and not y 
# D. x or not y 
# E. not x or y 
# F. not x or not y  
x,y = True,False

print(x and not y)
print(not x and y )
print(not x and not y )
print( x or not y )
print(not x or y )
print(not x or not y)

(12)
#Compare between two numbers and check whether they are equal, less than or greater than to each other.
items = ["aaa", 111, (4, 5), 2.01] # A set of objects
tests = [(4, 5), 3.14] # Keys to search for
for key in tests: # For all keys
    for item in items: # For all items
        if item == key: # Check for match
            print(key, "was found")
            break
        else:
            print(key, "not found!")


            x=input("enter x")
y=input("enter y")
if x == y: 
		print('x and y are equal') 
else: 
		if x < y: 
			print('x is less than y') 
		else: 
			print('x is greater')

      y=int(input("enter y"))
x = y // 2 					# For some y > 1
while x > 1:
		if y % x == 0: 				# Remainder
			print(y, 'has factor', x)
			break				 # Skip else
		x -= 1
else: 					# Normal exit
		print(y, 'is prime')
